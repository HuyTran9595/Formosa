using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Recipe_Slots : MonoBehaviour
{
    [Header("Produce")]
    public Products ProStat;
    [Header("Potions")]
    public Potions PotStats;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
